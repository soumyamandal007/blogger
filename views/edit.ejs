<html>
    <head>
        <title>Tech And Stuff</title>
         <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.14/semantic.min.css">
    </head>
    <body>
        <div class="ui main text container segment">
            <div class="huge header">Edit <strong><%= blog.title %></strong></div>
                <form class="ui form" action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
                    <div class="field">
                        <label class="">Title</label>
                        <input type="text" value="<%= blog.title %>" name="blog[title]">
                    </div>
                       <div class="field">
                        <label class="">Image</label>
                        <input type="text" value="<%= blog.image %>" name="blog[image]">
                    </div>
                       <div class="field">
                        <label class="">Blog Post</label>
                        <textarea name="blog[body]"><%= blog.body %></textarea>
                    </div>
                    <div class="field">
                         <label class="">Author</label>
                         <input type="text" value="<%= blog.author %>" name="blog[author]">
                    </div>
                      <input class="ui violet big basic button" type="submit">
                </form>
        </div>
    </body>
</html>